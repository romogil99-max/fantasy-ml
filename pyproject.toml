[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "fantasy-ml"
version = "0.1.0"
description = "Weekly ESPN fantasy football (PPR) point predictions from nflverse data"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
